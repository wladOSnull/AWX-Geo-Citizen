---

## for DB instance
- name: DB
  hosts: terraform_AmazonLinux2
  become_method: sudo
  roles:
     - SSITA_geocitizen_DB

## for server instance
- name: Server
  hosts: terraform_Ubuntu
  become_method: sudo
  become: yes
  roles:
    - SSITA_geocitizen_SERVER

...
